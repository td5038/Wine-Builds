From 743c2fc6e67318d129e248e1a05cdefe69c35f34 Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Sun, 3 May 2026 16:33:43 -0500
Subject: [PATCH 57/70] HACK: winewayland: Use version 5 of wl_seat.

This will fix high precision mice being overly sensitive in apps due to either wine bug or app bug (Haven't figured out which). Touchpad also works in the same way to maximize compatibility.

(cherry picked from commit 0ad4eb4cdcb53ea35691121f4ff8da9c54ddcbad)
---
 dlls/winewayland.drv/wayland.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/dlls/winewayland.drv/wayland.c b/dlls/winewayland.drv/wayland.c
index e41d5bda84b..73cf7bccd05 100644
--- a/dlls/winewayland.drv/wayland.c
+++ b/dlls/winewayland.drv/wayland.c
@@ -155,7 +155,7 @@ static void registry_handle_global(void *data, struct wl_registry *registry,
         }
         pthread_mutex_lock(&seat->mutex);
         seat->wl_seat = wl_registry_bind(registry, id, &wl_seat_interface,
-                                         version < 8 ? version : 8);
+                                         version < 5 ? version : 5);
         seat->global_id = id;
         wl_seat_add_listener(seat->wl_seat, &seat_listener, NULL);
         pthread_mutex_unlock(&seat->mutex);
-- 
2.54.0

