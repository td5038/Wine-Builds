From 18414b3b57857d2176e3f02b8d74a425b219f903 Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Thu, 15 Jan 2026 23:39:53 -0500
Subject: [PATCH 44/65] user32: Implement dead key support for
 MapVirtualKeyExA.

---
 dlls/user32/input.c | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

diff --git a/dlls/user32/input.c b/dlls/user32/input.c
index d375a4be782..c8fa4010408 100644
--- a/dlls/user32/input.c
+++ b/dlls/user32/input.c
@@ -269,18 +269,22 @@ UINT WINAPI MapVirtualKeyW(UINT code, UINT maptype)
  */
 UINT WINAPI MapVirtualKeyExA(UINT code, UINT maptype, HKL hkl)
 {
+    UINT top_bit = 0;
     UINT ret;
 
     ret = NtUserMapVirtualKeyEx( code, maptype, hkl );
     if (maptype == MAPVK_VK_TO_CHAR)
     {
         BYTE ch = 0;
-        WCHAR wch = ret;
+        WCHAR wch;
+
+        top_bit = ret & (1u << 31);
+        wch = ret & ~top_bit;
 
         WideCharToMultiByte( CP_ACP, 0, &wch, 1, (LPSTR)&ch, 1, NULL, NULL );
         ret = ch;
     }
-    return ret;
+    return ret | top_bit;
 }
 
 /****************************************************************************
-- 
2.54.0

